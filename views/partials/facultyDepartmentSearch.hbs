<form action="/search" method="GET" class="faculty-department-search">
  <div class="term-group">
    <label class="sr-only" for="q">Search Keyword</label>
    <input class="form-control" type="text" placeholder="Search Name or Keyword" id="q" name="q"/>
  </div>
  <span class="word-in">in</span>
  <div class="department-group">
    <label class="sr-only" for="department">Search for faculty in department</label>
    <input type="hidden" id="college" name="college" value=""/>
    <div class="input-group">
      <select class="select-department form-control" id="department" name="department">
        <option></option>
        {{#each organization}}
          <option class="college" value="{{this.college}}">{{this.college}}</option>
          {{#each this.departments}}
            <option class="department" value="{{this}}">{{this}}</option>
          {{/each}}
        {{/each}}
      </select><!--
      --><button class="btn-fp btn-search form-control" type="submit">
        <i class="search-icon fa fa-search"></i>
        <span class="button-label">Search</span>
      </button>
    </div>
  </div>
</form>
